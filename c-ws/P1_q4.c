/*Luiz Rodrigo Lacé Rodrigues 118049873*/

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
/* 4------não deu tempo de fazer---*/
int main(void) {
    char nome[50][128];
    int i, j, contador=0, pos[1024], temp;
    float nota[???];
    while(1){
        scanf("%s", nome[contador]);
        scanf("%f", &nota[contador]);
        if(nota[contador] < 0 || contador == 1023)
        {
        printf("-1")
        }
        pos[contador] = contador;
        contador++;
    };
    /* Ordena aqui */
    for(i=0; i < contador; i++){
    /* Imprime aqui */
    }
    return 0;
}